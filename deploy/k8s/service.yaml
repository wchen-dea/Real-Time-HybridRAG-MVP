apiVersion: v1
kind: Service
metadata:
  name: hybridrag-mcp
  namespace: hybridrag
spec:
  type: ClusterIP
  selector: {app: hybridrag-mcp}
  ports:
    - name: http
      port: 80
      targetPort: 8088
