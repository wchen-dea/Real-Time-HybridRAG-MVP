bundle:
  name: real-time-hybridrag-mvp
include:
  - resources/jobs/*.yml
targets:
  dev:
    mode: development
    default: true
    workspace:
      host: ${var.DATABRICKS_HOST_DEV}
  prod:
    mode: production
    workspace:
      host: ${var.DATABRICKS_HOST_PROD}
